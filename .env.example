# Адреса бекенд-API. Локально — типово http://localhost:4000.
# Для продакшну (GitHub Pages) задайте URL задеплоєного бекенду тут або через
# змінну репозиторію VITE_API_URL (див. .github/workflows/deploy.yml).
VITE_API_URL=http://localhost:4000
