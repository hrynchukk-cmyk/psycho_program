import { createNavigationContainerRef } from '@react-navigation/native'

// Глобальне посилання на навігацію — щоб переходити по натисканню на пуш.
export const navRef = createNavigationContainerRef()
